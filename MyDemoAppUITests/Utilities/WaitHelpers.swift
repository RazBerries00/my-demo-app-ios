//
//  WaitHelpers.swift
//  MyDemoAppUITests
//
//  Custom wait helpers and XCUIElement extensions
//

import XCTest

extension XCUIElement {

    // TODO: Add custom wait helpers here
    // Examples:

    // func waitAndTap(timeout: TimeInterval = TestConfig.defaultTimeout) {
    //     _ = waitForExistence(timeout: timeout)
    //     tap()
    // }

    // func waitAndType(_ text: String, timeout: TimeInterval = TestConfig.defaultTimeout) {
    //     _ = waitForExistence(timeout: timeout)
    //     tap()
    //     typeText(text)
    // }

    // func isVisible(timeout: TimeInterval = TestConfig.defaultTimeout) -> Bool {
    //     return waitForExistence(timeout: timeout) && isHittable
    // }
}
